
simulation:
  iso_samples: 1000
  robustness_samples: 200
  robustness_steps: 3
  base_noise: 0.0
  max_noise: 0.5
  
patient_generation:
  alpha_leak: 0.15 # Probability of GAS symptoms leaking through Carrier state
  
metrics:
  mcisaac_threshold: 3 # Score >= 3 is considered positive
  
paths:
  model_config: "../../../src/model_config.yaml"
  output_report: "benchmark_report.png"
